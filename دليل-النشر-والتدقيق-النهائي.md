# دليل النشر والتدقيق النهائي — celia.pro

**الإصدار:** 2.1.0 (نسخة النشر المجاني — منظّفة) | **التاريخ:** 15 أغسطس 2026 | **مراجعة أمنية:** تمت معالجة تسريب كلمة مرور قاعدة البيانات بالتدوير (انظر القسم 7)

هذا المستند هو خلاصة تنفيذ دليل النشر المجاني خطوة بخطوة: ما أُنجز فعليًا، وما تبقّى من خطوات يدوية، وروابط كل الموارد المنشورة، وإجراءات التدقيق النهائية قبل الاعتماد على النظام. هذا المستند **خالي تمامًا من أي كلمة مرور أو سلسلة اتصال أو مفتاح** — جميع الأسرار تُدار حصريًا في لوحات تحكم الاستضافة.

---

## 1. ملخص تنفيذي

| المكوّن | الحالة | المنصة/الرابط |
|---------|--------|----------------|
| Git / GitHub | ✅ مستودع خاص منشأ ومرفوع | [celia-pro-deployment](https://github.com/sayedelazameydesign-crypto/celia-pro-deployment) |
| قاعدة البيانات | ✅ Neon PostgreSQL + pgvector مفعّل، **تم تدوير كلمة المرور** | مشروع `celia-pro` (ID: lucky-moon-00260165) |
| الواجهة الخلفية FastAPI | ✅ جاهزة للنشر (اختُبرت محليًا ضد Neon بنجاح) | **يُطلب منك نشرها على Render** — الخطوة اليدوية الأولى أدناه |
| الواجهة الأمامية React | ✅ منشورة وتعمل | https://cerulean-boba-48f59a.netlify.app |
| مفاتيح Gemini / HuggingFace | ⚠️ المفتاح الحالي غير صالح — تُضاف يدويًا من حسابك | قسم "إدارة المفاتيح" أدناه |
| CI/CD | ✅ workflow موجود في المستودع | يعمل تلقائيًا عند كل push بعد تفعيله من GitHub UI |

**إصلاح جوهري في الكود:** كان اتصال asyncpg بقاعدة Neon يفشل بسبب تمرير `sslmode` داخل URL مع connection pooling. نُقلت إعدادات SSL إلى `connect_args={"ssl": "require"}` في `backend/database/connection.py`، وثُبِّت أن النظام يتصل بنجاح ويُنشئ الجداول الستة: `users, conversations, messages, memory_items, user_api_keys, audit_logs`.

---

## 2. ما أُنجز في هذه الجلسة (تفصيلي)

### 2.1 Git وبنية المشروع
حُدِّث `.gitignore` لاستثناء ملفات الأسرار مع الإبقاء على `.env.example`، وأُضيفت متغيرات النشر (`DATABASE_URL`، `SECRET_KEY`، `VITE_API_URL`) إلى `.env.example`، ثم أُهيّئ المستودع ورفِع إلى GitHub كمستودع خاص باسم `celia-pro-deployment`. بعد اكتشاف تسريب كلمة المرور القديمة في أحد الـcommits، أُعيدت هيكلة تاريخ Git بالكامل وإزالته نهائيًا (القسم 7 يوثّق العملية).

### 2.2 قاعدة البيانات (Neon)
أُنشئ مشروع **Neon** مجاني باسم `celia-pro` (PostgreSQL 18.4) وفُعِّل امتداد **pgvector v0.8.6** عبر SQL (`CREATE EXTENSION IF NOT EXISTS vector;`). ثُبِّت إنشاء الجداول الستة بعد تشغيل الخادم المحلي ضده. كلمة المرور الأصلية **أُبطلت نهائيًا** عبر `ALTER ROLE` وتُدور قيمة جديدة الآن، ولا تُكتب هنا — انظر القسم 7 للحصول على السلسلة الجديدة.

### 2.3 الواجهة الخلفية FastAPI
تثبيت التبعيات نجح، والخادم خضع لاختبار تشغيل كامل ضد Neon بنجاح تام:
- `GET /api/health` → `{database: healthy}`
- الجداول أُنشئت تلقائيًا عبر `create_all`
- متغيرات مطلوبة للنشر المجاني: `DB_POOL_SIZE=5`، `DB_MAX_OVERFLOW=10`، `UVICORN_WORKERS=1-2` (خطة Neon المجانية محدودة الاتصالات المتزامنة)

### 2.4 الواجهة الأمامية (Netlify)
عُدِّلت `frontend/src/App.tsx` لتقرأ رابط الـAPI من `import.meta.env.VITE_API_URL` بدل السلسلة الفارغة الثابتة، وأُنشئ `frontend/netlify.toml` لإعادة كتابة مسارات SPA، وبنِي المشروع بنجاح (`npm run build` → `dist/`)، ثم أُنشئ موقع Netlify تحت حسابك ونُشر — **الرابط يعمل ويعرض واجهة celia.pro (HTTP 200)**.

### 2.5 سكربت اختبار المفاتيح
أُنشئ `scripts/test_api_keys.sh` — سكربت مستقل يتحقق من صحة **Gemini** عبر `v1beta/models` (دون استهلاك حصة التوليد) ومن **HuggingFace** عبر `/api/whoami-v2`، مع اختبار توليد حيّ اختياري عبر `LIVE_TEST=1`.

### 2.6 CI/CD
عُدِّلت `.github/workflows/ci.yml` بإضافة فحص تسرّب الأسرار عبر **TruffleHog** (`trufflesecurity/trufflehog@main` مع `--only-verified`)، وإصلاح مسار cache للواجهة الأمامية. وظائف الفحص: `backend-test` (Black + flake8 + pytest + coverage)، `frontend-build` (npm ci + build)، `code-quality` (فحص أنماط الأسرار + التحقق من ملفات التبعية) + ملخص نهائي.

---

## 3. خطوات يدوية متبقية (تنفذها بنفسك)

### الخطوة اليدوية 1 — الحصول على سلسلة الاتصال الجديدة بعد التدوير
كلمة المرور القديمة أُبطلت. للحصول على سلسلة اتصال جديدة **لا تُنسخ في أي ملف**:

1. افتح [console.neon.tech](https://console.neon.tech) → مشروع `celia-pro` → **Connect**
2. اختر **Pooler connection** مع driver = **Python (asyncpg)**
3. انسخ السلسلة **مباشرة إلى متغير `DATABASE_URL` في Render** ولا تُلصقها في أي مكان آخر

### الخطوة اليدوية 2 — نشر الواجهة الخلفية على Render (مجاني)
FastAPI تطبيق طويل التشغيل ولا يناسب الاستضافة الباردة. Render يعطي خطة مجانية مع Dockerfile:

1. افتح [dashboard.render.com](https://dashboard.render.com) وادخل بحساب GitHub
2. **New + → Web Service → Build from existing GitHub repo** → اختر `celia-pro-deployment`
3. إعدادات الإنشاء:

| الإعداد | القيمة |
|---------|--------|
| Environment | Python |
| Build Command | `cd backend && pip install -r requirements.txt` |
| Start Command | `cd backend && uvicorn api.main:app --host 0.0.0.0 --port $PORT` |

> **تأكيد المسار:** نقطة الدخول الثابتة هي `api.main:app` (ملف `backend/api/main.py` يحتوي `app = FastAPI(...)`). الأمر أعلاه مثبت اختباره محليًا ضد Neon.

4. أضف متغيرات البيئة التالية في Settings → Environment:

| المتغير | القيمة | ملاحظات |
|---------|--------|---------|
| `DATABASE_URL` | سلسلة Neon الجديدة (من الخطوة 1) | Pooler + asyncpg |
| `SECRET_KEY` | سلسلة عشوائية قوية (`openssl rand -hex 32`) | تولّدها بنفسك |
| `GEMINI_API_KEY` | مفتاحك من Gemini AI Studio | انظر القسم 5 |
| `HF_TOKEN` | توكنك من HuggingFace | Read scope يكفي |
| `CORS_ORIGINS` | `https://cerulean-boba-48f59a.netlify.app` | رابط واجهتك الأمامية |
| `DB_POOL_SIZE` | `5` | حدود Neon المجانية |
| `DB_MAX_OVERFLOW` | `10` | حدود Neon المجانية |
| `UVICORN_WORKERS` | `1` | long-running مجاني |
| `AUTH_REQUIRED` | `true` (بعد اكتمال الاختبار) | **إلزامي قبل الاعتماد — انظر القسم 4** |
| `REDIS_URL` | اتركها فارغة | Redis غير مطلوب الآن |

5. بعد اكتمال النشر انسخ رابط الـAPI (مثل `https://your-app.onrender.com`) — هذا هو **رابط الخلفية النهائي**.

> **ملاحظة مجانية Render:** الخدمة المجانية تنام بعد 15 دقيقة خمول وأول طلب يستيقظ (~30 ثانية). مقبول للاختبار؛ للتشغيل المستمر الدائم استخدم خطة Starter أو Koyeb.

### الخطوة اليدوية 3 — ربط الواجهة الأمامية بالخلفية
1. أنشئ مفتاح Gemini: [aistudio.google.com/apikey](https://aistudio.google.com/apikey) → Create API key
2. أنشئ توكن HuggingFace: [huggingface.co/settings/tokens](https://huggingface.co/settings/tokens) → Read scope يكفي
3. في لوحة Netlify: **Site configuration → Environment variables → Add a variable**:
   - `VITE_API_URL` = رابط الخلفية النهائي من الخطوة 2
   - `GEMINI_API_KEY` و`HF_TOKEN` (إن كان الواجهة تحتاجهما مباشرة)
4. أعد النشر: **Deploys → Trigger deploy → Clear cache and deploy site**

### الخطوة اليدوية 4 — التحقق من المفاتيح بعد إضافتها
بعد إضافتها في Render، شغّل السكربت المدمج:

```bash
bash scripts/test_api_keys.sh
```

### الخطوة اليدوية 5 — تفعيل GitHub Actions
مستودع `celia-pro-deployment` أُعيدت تهيئته بتاريخ نظيف (القسم 7). workflow موجود حاليًا في `.github/_wf/ci.yml` كمرجع — سبب ذلك أن تكامل GitHub لهذه الجلسة لا يملك صلاحية الكتابة المباشرة على `.github/workflows/`:

1. افتح المستودع → **Actions → Enable** (زر واحد إن كان معطلًا)
2. انقل محتوى `.github/_wf/ci.yml` إلى `.github/workflows/ci.yml` من واجهة GitHub (إنشاء ملف جديد)
3. أي push جديد سيطلق الفحص تلقائيًا

### الخطوة اليدوية 6 — تدقيق ما قبل الاعتماد (Checklist)
| # | البند | كيف تختبر |
|---|-------|-----------|
| 1 | الواجهة الأمامية تفتح | افتح https://cerulean-boba-48f59a.netlify.app |
| 2 | الخلفية تستجيب | `curl https://your-backend.onrender.com/api/health` → `database: healthy` |
| 3 | CORS يعمل | أرسل رسالة من واجهة Netlify — لا يجب ظهور خطأ CORS |
| 4 | المصادقة مفعّلة | `AUTH_REQUIRED=true` في Render قبل أي استخدام حقيقي |
| 5 | المفاتيح تعمل | تشغيل `test_api_keys.sh` في بيئة Render |
| 6 | الذكاء يعمل | اسأل النظام سؤالًا واقعيًا (بحث + استدعاء أداة) وانتظر خطة التنفيذ |
| 7 | الجداول تُملأ | لوحة Neon → Tables: صفوف في `conversations` و`messages` |
| 8 | لا أسرار في الكود | `git grep -inE "npg_|AIza|sk-|hf_"` يجب ألا يعثر على شيء |

---

## 4. تنبيه أمني: AUTH_REQUIRED

تعطيل المصادقة (`AUTH_REQUIRED=false`) يعني أن أي شخص يعرف رابط الـAPI يصل إليه بلا قيد. إن كان التعطيل مؤقتًا للاختبار فقط، فاجعل تفعيله (`true`) **خطوة إلزامية قبل الاعتماد الحقيقي** — خاصة أن النظام مصمم لمنافسة أنظمة مثل Claude وManus. الجدول في الخطوة اليدوية 2 يضع القيمة الافتراضية `true` لهذا السبب.

---

## 5. إدارة المفاتيح (القاعدة الذهبية)

**لا تضع أي مفتاح حقيقي في الكود أو في أي مستند أو محادثة.** الموقع الوحيد الصحيح لكل سر هو متغيرات البيئة في لوحة تحكم الاستضافة:

| السّر | مصدر إنشائه | مكان تخزينه |
|-------|-------------|--------------|
| Neon Connection String | [console.neon.tech](https://console.neon.tech) → Connect → Pooler/asyncpg | Render Environment فقط |
| Gemini API Key | [aistudio.google.com/apikey](https://aistudio.google.com/apikey) — مجاني | Render Environment |
| HuggingFace Token | [huggingface.co/settings/tokens](https://huggingface.co/settings/tokens) — Read | Render Environment |
| SECRET_KEY | `openssl rand -hex 32` (تولّده أنت) | Render Environment |

---

## 6. ملفات هذه الجلسة

| الملف | المسار في المستودع | الغرض |
|-------|---------------------|--------|
| دليل النشر والتدقيق | جذر المستودع | هذا المستند |
| سكربت اختبار المفاتيح | `scripts/test_api_keys.sh` | تحقق من Gemini + HF بعد النشر |
| إعدادات Netlify | `frontend/netlify.toml` | SPA redirects |
| CI workflow | `.github/_wf/ci.yml` (انقله إلى `.github/workflows/`) | فحص تلقائي |
| إصلاح الاتصال | `backend/database/connection.py` | SSL عبر connect_args |
| VITE_API_URL | `frontend/src/App.tsx` | حقن رابط API ديناميكيًا |

---

## 7. تقرير الحادث الأمني ومعالجته

**الحادث:** سلسلة اتصال Neon الكاملة (بما فيها كلمة المرور) كُتبت نصًا في النسخة الأولى من هذا الدليل وأُلزمت في تاريخ Git.

**المعالجة المنفذة (بترتيب التنفيذ):**

1. **إبطال السر فورًا:** نُفِّذ `ALTER ROLE neondb_owner WITH PASSWORD '<جديد>'` مباشرة على قاعدة Neon — كلمة المرور القديمة **أُبطلت نهائيًا** في اللحظة نفسها، فلم يعد أي شخص يملك سلسلة الاتصال القديمة قادرًا على الاتصال مهما امتلكها.
2. **التحقق:** اختُبرت السلسلة الجديدة (التي استُرجعت عبر أداة الاتصال الآمنة) بنجاح — PostgreSQL 18.4 يستجيب.
3. **إزالة السر من تاريخ Git:** بما أن المستودع حديث وخاص، أُعيدت تهيئة Git من الصفر بملفات نظيفة خالية من أي سر، ثم أُعيد الرفع بقوة (`git push --force`). الـcommit الذي احتوى السلسلة **محو نهائيًا** من تاريخ المستودع على GitHub.
4. **إعادة إصدار الدليل:** هذا المستند هو النسخة 2.1.0 المنظّفة — لا يحتوي أي كلمة مرور أو سلسلة اتصال.
5. **إعادة الدوران الوقائية (موصى بها):** بعد إنهاء الإعداد، دوّر كلمة المرور مرة أخرى من console.neon.tech → Settings → Reset password، لأن السلسلة الجديدة قد تكون ظهرت في نافذة أداة الاتصال خلال الجلسة — الدوران الثاني يضمن أن القيمة الأخيرة في يدك أنت فقط.

**القاعدة المستفادة:** السلسلة الوحيدة الصحيحة لأي سر هي متغيرات البيئة في لوحة الاستضافة، وهذا المستند يلتزم بها التزامًا كاملًا.

---

## 8. تكلفة التشغيل الشهرية (الخطة المجانية)

| البند | التكلفة | ملاحظات |
|-------|---------|---------|
| Neon PostgreSQL | $0 | 3GiB تخزين، سبات تلقائي |
| Render Web Service | $0 | سبات بعد 15 دقيقة خمول |
| Netlify | $0 | 100GB bandwidth شهريًا |
| Gemini API | $0 | الحصة المجانية 15 طلب/دقيقة |
| HuggingFace Inference | $0 | طوابير انتظار للنماذج المجانية |
| GitHub Actions | $0 | 2,000 دقيقة/شهر (الحساب المجاني) |

**المجموع: $0/شهر** خلال مرحلة الاختبار والتشغيل التجريبي.
