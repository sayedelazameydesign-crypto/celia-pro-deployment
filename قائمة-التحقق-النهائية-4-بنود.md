# قائمة التحقق النهائية — لحظة الاكتمال الحقيقية

> 4 بنود فقط. إذا تحققت الأربعة جميعًا، فالنظام منشور ويعمل من طرف إلى طرف.
> أي بند غير محقق = النشر لم يكتمل بعد.

---

## ✅ البند 1 — الخلفية حيّة على Render

**الاختبار:**

```bash
curl https://your-app.onrender.com/api/health
```

**النجاح =** يرد `{"database": "healthy"}` (وليس خطأ CORS ولا مهلة ولا 404).

> هذا هو قلب النظام. بدونه تكون واجهة Netlify قشرة فارغة بلا عقل خلفها.

---

## ✅ البند 2 — المفاتيح صالحة ومضافة في Render فقط

**الاختبار:**

```bash
bash scripts/test_api_keys.sh
```

**النجاح =** `GEMINI_API_KEY: ✅ صالح` و `HF_TOKEN: ✅ صالح`.

> شروط إلزامية: المفتاحان **جديدان** (ليس المفتاح الذي رجع `API_KEY_INVALID`)،
> ومكانهما **حقل متغيرات البيئة في Render فقط** — لا في الكود ولا في أي محادثة.

---

## ✅ البند 3 — الرسالة تمر من طرف إلى طرف

**الاختبار:** افتح `https://cerulean-boba-48f59a.netlify.app` وأرسل سؤالًا واقعيًا
(بحث + استدعاء أداة) وانتظر ردًا فعليًا.

**النجاح =** ردّ ذكي حقيقي، **بلا خطأ CORS**، وصف جديد ظهر في جدولَي
`conversations` و`messages` في لوحة Neon.

> الشرط الضمني: `VITE_API_URL` في Netlify يشير إلى رابط الخلفية النهائي،
> و `AUTH_REQUIRED=true` في Render قبل أي استخدام حقيقي.

---

## ✅ البند 4 — لا أثر لأي سر في أي مكان

**الاختبار:**

```bash
git grep -inE "npg_|AIza|hf_[A-Za-z0-9]|sk-" -- . ':!*.md' || echo "لا أسرار"
```

**النجاح =** يطبع `لا أسرار`.

> المفاتيح وسلسلة الاتصال تعيش في مكان واحد فقط: متغيرات بيئة Render.
> إن ظهر أي أثر في المستودع أو المحادثة، دوّر فورًا ولا تكمل.

---

## الخلاصة

| البند | ما يثبته |
|---|---|
| 1 | الخلفية منشورة ومتصلة بقاعدة البيانات |
| 2 | قدرة الذكاء الاصطناعي فعّالة |
| 3 | الواجهة والخلفية والقاعدة تعمل سلسلة واحدة |
| 4 | السرّ محتجز في مكانه الصحيح |

**الأربعة مكتملة = النشر المجاني اكتمل.**
**أي بند ناقص = أكمله أولًا، ثم أعد التحقق من هذا البند وحده.**
