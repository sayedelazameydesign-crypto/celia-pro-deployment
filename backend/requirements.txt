# celia.pro Backend Dependencies
# Updated for v3.0.0 - Multi-tenant with Authentication

# ============= CORE =============
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
pydantic>=2.5.0
pydantic[email]>=2.5.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
websockets>=17.0
numpy>=1.24.0            # For embeddings and vector operations

# ============= DATABASE =============
sqlalchemy>=2.0.0
asyncpg>=0.31.0          # PostgreSQL async driver
aiosqlite>=0.20.0        # SQLite async driver (for development)
alembic>=1.19.0          # Database migrations

# ============= AUTHENTICATION =============
python-jose[cryptography]>=3.5.0  # JWT tokens
passlib[bcrypt]>=1.7.4            # Password hashing

# ============= EMBEDDINGS =============
fastembed>=0.8.0         # Semantic embeddings (ONNX-based, lightweight)

# ============= MONITORING =============
prometheus-fastapi-instrumentator>=7.0.0  # Prometheus metrics for FastAPI
prometheus-client>=0.20.0                  # Prometheus client library
structlog>=24.1.0                          # Structured logging
sentry-sdk[fastapi]>=1.40.0               # Error tracking (optional)

# ============= OPTIONAL =============
# redis>=5.0.0           # For session management and caching
# python-multipart>=0.0.6  # For file uploads
# boto3>=1.34.0          # For S3 file storage
