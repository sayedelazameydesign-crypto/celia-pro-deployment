# دليل النشر المجاني 100% بدون فيزا — celia.pro

> **الحقيقة المؤكدة (أغسطس 2026):** Cloud Run وHuggingFace Spaces (Docker) وFly.io وRender وRailway وOracle كلها تتطلب بطاقة. المتبقيان الحقيقيان بلا فيزا: **Koyeb** (عادة لا يطلب بطاقة للـfree tier) و**Zeabur** (لا بطاقة رسميًا — [Free Plan docs](https://zeabur.com/docs/en-US/pricing/free-plan)).

## المسار الأساسي: Koyeb (أكمل من حيث توقفت)

حسابك في Koyeb سبق أن وصل مرحلة الإنشاء دون طلب بطاقة — **لا تُغلق الحساب القديم، أكمل عليه.**

| الحقل في Koyeb | القيمة |
|----------------|--------|
| GitHub repo | `sayedelazameydesign-crypto/celia-pro-deployment` / `main` |
| Root Directory | `backend` |
| Build Command | `pip install -r requirements.txt` |
| Run Command | `uvicorn main:app --host 0.0.0.0 --port $PORT` |
| Port | 8000 (HTTP) |

المتغيرات التسعة (Add Secret في لوحة Koyeb — لا تُرسلها في أي محادثة):

| المتغير | المصدر |
|---------|--------|
| DATABASE_URL | لوحة Neon → celia-pro → Connect (السلسلة الجديدة بعد التدوير) |
| JWT_SECRET_KEY | القيمة التي أنشأتها سابقًا |
| GEMINI_API_KEY | aistudio.google.com/app/apikey (مفتاح جديد) |
| GROQ_API_KEY | console.groq.com/keys (مفتاح جديد) |
| HF_TOKEN | huggingface.co/settings/tokens (توكن قراءة) |
| CORS_ORIGINS | `https://cerulean-boba-48f59a.netlify.app` |
| DB_POOL_SIZE | `5` |
| DB_MAX_OVERFLOW | `10` |
| AUTH_REQUIRED | `true` |

ملاحظة Koyeb: الخدمة المجانية تنام بعد ساعة خمول (scale-to-zero) — أول طلب يستيقظ خلال ثوانٍ، هذا طبيعي.

## المسار الاحتياطي المضمون: Zeabur (لا فيزا رسميًا)

1. سجّل عبر GitHub: https://zeabur.com (No credit card required)
2. New Project → **Deploy from GitHub** → اختر `celia-pro-deployment`
3. Zeabur يكتشف `Dockerfile` في الجذر (Python buildpack أو Docker — اختر Docker)
4. بعد البناء: لوحة الخدمة → **Variables** → أضف المتغيرات التسعة أعلاه
5. **Redeploy** → احصل على الرابط (مثل `https://xxxx.zeabur.app`)

ملاحظة Zeabur: الخدمة تنام بعد الخمول وتستيقظ بطلب جديد (cold start ثوانٍ) — نفس سلوك Koyeb المجاني.

## بعد Live (في المسارين)

أرسل لي الرابط فقط وسأنفذ فورًا:

1. `curl /api/health` → `database: healthy` + تأكيد امتلاء قاعدة Neon بالجداول
2. CORS preflight من رابط Netlify
3. تسجيل → تسجيل دخول → JWT → رسالة Agent واقعية → صف في Neon
4. تدقيق نهائي: `git grep` لا أسرار
5. تحديث Netlify: `VITE_API_URL=https://رابط-الخلفية` + إعادة نشر
6. تقرير **DEPLOYMENT STATUS** (PASS/FAIL على 4 بنود)
