# برومبت "النشر النهائي مع قفل الأسرار"

انسخ البرومبت التالي بالكامل وألصقه في جلسة وكيل محلية جديدة (مع إرفاق `celia_pro_secure.zip` من المستودع). البرومبت مكتوب بحيث **يتوقف الوكيل عند كل سر ويقف عند حقل متغيرات البيئة** — لا يطلب منك لصق أي سر في المحادثة إطلاقًا.

---

## البرومبت

```
نفّذ "النشر النهائي مع قفل الأسرار" لمشروع celia.pro (المرفق) وفق دليل النشر والتدقيق النهائي v2.2.0 الموجود في جذر المشروع.

قانونان لا استثناء فيهما:
1. لا تطلب مني قط لصق أو كتابة أو إرسال أي سر (كلمة مرور، سلسلة اتصال، API key) داخل المحادثة — لا في النص ولا في الملفات.
2. الموقع الوحيد الصحيح لأي سر هو متغيرات البيئة في لوحة تحكم الاستضافة، وأنت توقف التنفيذ عند حقل المتغير حتى أكمله أنا بنفسي.

تنفيذ هذا البرومبت = تنفيذ القسمين 3 و4 من الدليل حرفيًا:

أ. الخطوة اليدوية 1 (قفل الأسرار):
   - لا تسترجع أي سلسلة اتصال من أي أداة.
   - علّمني خطوة واحدة: ادخل console.neon.tech → مشروع celia-pro → Connect → Pooler/asyncpg → انسخ السلسلة → الصقها مباشرة في حقل DATABASE_URL في لوحة Render. توقّف عندي هنا ولا تكتب السلسلة في أي ملف أو رسالة.

ب. الخطوة اليدوية 2 (نشر الواجهة الخلفية على Render):
   - أنشئ Web Service من مستودع GitHub celia-pro-deployment.
   - Environment: Python
   - Build Command: cd backend && pip install -r requirements.txt
   - Start Command: cd backend && uvicorn api.main:app --host 0.0.0.0 --port $PORT
   - أدرج لي جدول متغيرات البيئة التالية دون قيمها، بحيث أملؤها أنا من لوحتي:
     DATABASE_URL (السلسلة الجديدة من لوحة Neon مباشرة)
     SECRET_KEY (ولّد قيمة: openssl rand -hex 32 — هذه تولّدها وتعرضها لي مرة واحدة لألصقها)
     GEMINI_API_KEY (من aistudio.google.com/apikey)
     HF_TOKEN (من huggingface.co/settings/tokens — Read يكفي)
     CORS_ORIGINS=https://cerulean-boba-48f59a.netlify.app
     DB_POOL_SIZE=5، DB_MAX_OVERFLOW=10، UVICORN_WORKERS=1، AUTH_REQUIRED=true
   - توقّف بعد تقديم الجدول حتى أكمل اللصق في لوحة Render.

ج. الخطوة اليدوية 3 (ربط الواجهة الأمامية):
   - في لوحة Netlify (موقع cerulean-boba-48f59a): أضف متغير VITE_API_URL = رابط الخلفية الجديد من Render.
   - أعد النشر: Deploys → Trigger deploy → Clear cache and deploy site.
   - توقّف حتى يثبت لي أن الواجهة تعمل (أنسخ لك رابط /api/health من الخلفية لترد عليه).

د. الخطوة اليدوية 5 (GitHub Actions):
   - فعّل Actions من تبويب Actions في المستودع.
   - أنشئ .github/workflows/ci.yml من واجهة GitHub بانسخ محتوى .github/_wf/ci.yml (خطوة لمرة واحدة).

هـ. الخطوة اليدوية 6 (التدقيق):
   - نفّذ checklist القسم 3 من الدليل بندًا بندًا، وعرض لي النتيجة في جدول: ✅/❌ لكل بند من 8.

قبل البدء: ألصق لك رابط /api/health النهائي من لوحة Render بعد إتمام النشر — لا تطلب مني أي سر قبلها وبعدها.
```

---

## لماذا هذا البرومبت آمن؟

| آلية | الضمان |
|------|--------|
| قانونان مطلقان في أول البرومبت | الوكيل ممنوع قانونيًا من طلب أي سر في المحادثة |
| التوقف عند كل حقل متغير | السلسلة تنتقل من لوحة Neon إلى حقل DATABASE_URL في Render فقط، بدون وسيط |
| التحقق بعد النشر بدلًا من التحقق بمفاتيح مسربة | تختبر بـ `/api/health` علني لا بأس فيه |
| SECRET_KEY يولّده الوكيل ويعرضه مرة واحدة | لا يُسجَّل في أي ملف؛ أنت تلصقه مباشرة |
