# خطوات إتمام النشر المجاني بالكامل — celia.pro

> هذه هي القائمة الوحيدة المرجعية. تسع خطوات مرقّمة بالترتيب، كل خطوة تتوقف عند حقل المتغير حتى تكمله أنت — لا تُلصق أي سر في أي مكان سوى حقل متغيرات البيئة في Render.

---

## الخطوة 1 — تدوير كلمة المرور فورًا (إلزامية — أولوية قصوى)

سلسلة اتصال بكلمة مرورها ظهرت في سجل المحادثة، لذا **كلمة المرور الحالية صارت مكشوفة ويجب إبطالها قبل أي شيء آخر**:

1. افتح [console.neon.tech](https://console.neon.tech) وسجّل الدخول
2. ادخل مشروع **celia-pro** (ID: `lucky-moon-00260165`)
3. من القائمة الجانبية: **Settings → General → Reset password** (أو من **Database → Roles → neondb_owner → Reset password**)
4. بعد التدوير تُعرض سلسلة جديدة — **لا تُلصقها هنا ولا في أي ملف**: انقلها مباشرة إلى الخطوة 2

> هذه الخطوة تستغرق دقيقة واحدة فقط، وهي أول ما يجب أن تفعله الآن.

---

## الخطوة 2 — إنشاء خدمة الخلفية على Render

1. افتح [dashboard.render.com](https://dashboard.render.com) وسجّل الدخول بحساب GitHub
2. **New + → Web Service**
3. **Build from existing GitHub repo** → اختر `celia-pro-deployment`
4. اضبط الإعدادات:

| الإعداد | القيمة |
|---------|--------|
| Environment | Python |
| Build Command | `cd backend && pip install -r requirements.txt` |
| Start Command | `cd backend && uvicorn api.main:app --host 0.0.0.0 --port $PORT` |
| Free Instance Type | نعم (المجاني) |

---

## الخطوة 3 — إضافة متغيرات البيئة في Render

من صفحة الخدمة على Render: **Settings → Environment → Add Environment Variable**، وأضف كل متغير ثم **Save Changes**:

| المتغير | القيمة | المصدر |
|---------|--------|--------|
| `DATABASE_URL` | السلسلة الجديدة من الخطوة 1 (بعد التدوير) | لوحة Neon → Connect → Pooler، driver: Python (asyncpg) |
| `SECRET_KEY` | `openssl rand -hex 32` — لوّلدها بنفسك في طرفيتك: شغّل الأمر وألصق الناتج | طرفيتك المحلية (Mac/Linux) |
| `GEMINI_API_KEY` | مفتاح جديد من [aistudio.google.com/apikey](https://aistudio.google.com/apikey) → Create API key | حسابك في Google |
| `HF_TOKEN` | توكن من [huggingface.co/settings/tokens](https://huggingface.co/settings/tokens) → Create → **Read** scope | حسابك في HuggingFace |
| `CORS_ORIGINS` | `https://cerulean-boba-48f59a.netlify.app` | رابط واجهتك الأمامية |
| `DB_POOL_SIZE` | `5` | حدود Neon المجانية |
| `DB_MAX_OVERFLOW` | `10` | حدود Neon المجانية |
| `UVICORN_WORKERS` | `1` | مناسب للخطة المجانية |
| `AUTH_REQUIRED` | `true` | إلزامي قبل أي استخدام حقيقي |

> بعد Save Changes يعيد Render بناء الخدمة تلقائيًا. انتظر حتى يظهر **Live** (قد يستغرق 3–5 دقائق).

---

## الخطوة 4 — تدوين رابط الخلفية

من صفحة الخدمة على Render، انسخ الرابط من أعلى الصفحة (نمطه `https://celia-pro.onrender.com`). هذا هو **رابط الواجهة الخلفية النهائي** — ستحتاجه في الخطوة 5.

---

## الخطوة 5 — ربط الواجهة الأمامية بالخلفية (Netlify)

1. افتح لوحة Netlify: [app.netlify.com/sites/cerulean-boba-48f59a](https://app.netlify.com/sites/cerulean-boba-48f59a)
2. **Site configuration → Environment variables → Add a variable**
3. أضف: الاسم `VITE_API_URL` — القيمة = رابط الخلفية من الخطوة 4 **بدون شرطة مائلة في النهاية** (مثل `https://celia-pro.onrender.com`)
4. أعد النشر: **Deploys → Trigger deploy → Clear cache and deploy site**

> **مهم:** `VITE_API_URL` يُحقن في الكود أثناء البناء، لذا يجب **إعادة البناء** (Trigger deploy) وليس مجرد حفظ المتغير.

---

## الخطوة 6 — التحقق الأول: الخلفية حيّة

انتظر حتى يستيقظ Render (أول طلب بعد السبات يستغرق ~30 ثانية — طبيعي). ثم:

```bash
curl https://<رابط-الخلفية>.onrender.com/api/health
```

**النجاح =** `{"database": "healthy"}`. عندئذٍ اقلب **البند 1** من قائمة التحقق إلى `✅`.

> إن واجهت خطأ 404 أو Timeout: تحقق أن Start Command مطابق تمامًا للخطوة 2، وافحص **Deploy log** في Render — وأرسل لي السطر الأخير من السجل.

---

## الخطوة 7 — التحقق الثاني: المفاتيح صالحة

بعد إضافة المفاتيح في الخطوة 3، تحقّق من عملها من طرفيتك ضد الخلفية المنشورة:

```bash
curl https://<رابط-الخلفية>.onrender.com/api/health
```

والتحقق من أن مفاتيحك الجديدة صالحة فعلًا قبل استخدامها (بدون إرسالها لأي أحد):

```bash
curl -s "https://generativelanguage.googleapis.com/v1beta/models?key=<GEMINI_API_KEY>&pageSize=3" | head -2
curl -s "https://huggingface.co/api/whoami-v2" -H "Authorization: Bearer <HF_TOKEN>" | head -2
```

**النجاح =** قائمة نماذج من Gemini وبيانات حسابك من HuggingFace. عندئذٍ اقلب **البند 2** إلى `✅`.

---

## الخطوة 8 — التحقق الثالث: طرف إلى طرف

1. افتح [cerulean-boba-48f59a.netlify.app](https://cerulean-boba-48f59a.netlify.app) في متصفحك
2. أرسل سؤالًا واقعيًا (مثل: «ابحث عن آخر أخبار الذكاء الاصطناعي اليوم ولخّصها»)
3. **النجاح =** ردّ ذكي حقيقي بلا خطأ CORS، وصف جديد ظهر في جدولَي `conversations` و`messages` في لوحة Neon. اقلب **البند 3** إلى `✅`.

> إذا ظهر خطأ CORS في وحدة تحكم المتصفح: تأكد أن `CORS_ORIGINS` في Render يطابق رابط Netlify حرفيًا (الخطوة 3).

---

## الخطوة 9 — GitHub Actions + التدقيق النهائي

1. **تفعيل Actions:** افتح المستودع → تبويب **Actions** → **Enable** (زر واحد)
2. **نقل الـworkflow:** من واجهة GitHub: **Add file → Create new file** → المسار `.github/workflows/ci.yml` → انسخ محتوى `.github/_wf/ci.yml` من المستودع → **Commit**
3. **التدقيق النهائي (البند 4):** في طرفيتك داخل نسخة المشروع:

```bash
git grep -inE "npg_|AIza[A-Za-z0-9_-]|hf_[A-Za-z0-9]{20,}|sk-[A-Za-z0-9]" -- . ':!*.md' || echo "لا أسرار"
```

**النجاح =** يطبع `لا أسرار`. اقلب **البند 4** إلى `✅`.

---

## جدول حالة الاكتمال

| البند | الاختبار | تحققه بعد الخطوة |
|---|---|---|
| ⬜ 1 — الخلفية حيّة | `curl /api/health` → `{"database":"healthy"}` | 6 |
| ⬜ 2 — المفاتيح صالحة في Render | `generativelanguage` + `whoami-v2` | 7 |
| ⬜ 3 — طرف إلى طرف | رسالة من Netlify → رد حقيقي + صف في Neon | 8 |
| ⬜ 4 — لا أسرار | `git grep` → `لا أسرار` | 9 |

**الأربعة `✅` = النشر المجاني اكتمل بالكامل.**

---

## قواعد مطلقة لا استثناء فيها

1. **السلسلة من لوحة Neon تذهب إلى حقل `DATABASE_URL` في Render فقط** — لا تُلصق في أي محادثة أو ملف أو وكيل
2. **`AUTH_REQUIRED=true` قبل أي استخدام حقيقي** — لا تؤجلها
3. **المفاتيح الجديدة فقط** — المفتاح القديم رجع `API_KEY_INVALID` فلا تعيد استخدامه
4. **السبات الأول طبيعي** — ~30 ثانية لاستيقاظ Render بعد خمول 15 دقيقة
